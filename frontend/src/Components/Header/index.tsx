import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <header>
                Logo
            </header>
        );
    }
}

export default Header;